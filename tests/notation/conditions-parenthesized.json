{
  "description": "Parentheses around conditions make no node, so parenthesized conditions joined by ∧ at the top are still separate conditions, each applying where its captures are (engine §9).",
  "document": "```ebnf\na ≔ $x(A) | $y(B) : (\"p\" ∈ tags($x) ∧ \"q\" ∈ tags($y)) ;\n```\n",
  "expect": {
    "dom": {
      "rules": [
        {
          "conditions": [
            {
              "op": "∈",
              "left": {
                "literal": "p"
              },
              "right": {
                "call": "tags",
                "args": [
                  {
                    "capture": "x"
                  }
                ]
              }
            },
            {
              "op": "∈",
              "left": {
                "literal": "q"
              },
              "right": {
                "call": "tags",
                "args": [
                  {
                    "capture": "y"
                  }
                ]
              }
            }
          ]
        }
      ]
    }
  }
}
