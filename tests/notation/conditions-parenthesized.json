{
  "description": "One condition of the list applies only to alternatives that capture everything it uses; one joining conditions on two alternatives' captures with ∧ applies to neither, which is an error at the rule (engine §3.6, §9).",
  "document": "```jbogenbau\n%rule a $x(A) | $y(B) %conditions \"p\" ∈ tags($x) ∧ \"q\" ∈ tags($y)\n```\n",
  "expect": {
    "error": {
      "line": 2,
      "column": 1
    }
  }
}
