{
  "description": "<> after a capture makes it silent, while the other captures are emitted (engine §9, §11).",
  "document": "```jbogenbau\n%rule a $x(A) $y(B) %emits $x <>, $y\n```\n",
  "expect": {
    "dom": {
      "rules": [
        {
          "emit": {
            "items": [
              {
                "capture": "x",
                "silent": true
              },
              {
                "capture": "y"
              }
            ]
          }
        }
      ]
    }
  }
}
