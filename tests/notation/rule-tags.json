{
  "description": "Tags on a rule's name apply to all its alternatives (engine §9).",
  "document": "# A document\n\nSome prose.\n\n```jbogenbau\n%rule cmavo $w(body) %tags \"cmavo\" ∪ tags($w, lexicon)\n```\n",
  "expect": {
    "dom": {
      "rules": [
        {
          "name": "cmavo",
          "tags": {
            "union": [
              {
                "literal": "cmavo"
              },
              {
                "call": "tags",
                "args": [
                  {
                    "capture": "w"
                  },
                  {
                    "rule": "lexicon"
                  }
                ]
              }
            ]
          }
        }
      ]
    }
  }
}
