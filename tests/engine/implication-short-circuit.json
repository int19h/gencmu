{
  "description": "A ⟹ B evaluates B only where A holds, so a nested parse that would ask about its own span is not run for a false premise (engine §10).",
  "grammar": "%rule text $a(A)\n%conditions text($a) = \"b\" ⟹ matches($a, text)",
  "tokens": [
    {
      "text": "a",
      "tags": [
        "A"
      ]
    }
  ],
  "expect": {
    "result": {
      "ok": true
    }
  }
}
